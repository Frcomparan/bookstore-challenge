<%= form_with(model: loan_book, class: 'mt-3') do |form| %>
  <% if loan_book.errors.any? %>
    <div class="alert alert-danger alert-dismissible">
      <h4><%= pluralize(loan_book.errors.count, "error") %> prohibited this loan_book from being saved:</h4>

      <ul>
        <% loan_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :book_id, 'Seleccione un libro' %>
    <%= form.collection_select :book_id, @books, :id, :name, {}, {class: 'form-control'} %>
  </div>

  <div class="form-group">
    <%= form.label :user_id, 'Seleccione un libro' %>
    <%= form.collection_select :user_id, @users, :id, :email, {}, {class: 'form-control'} %>
  </div>

  <div class="form-group">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>
